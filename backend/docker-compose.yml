version: "3.8"

services:
  backend:
    image: my-backend
    env_file:
      - .env
    expose:
      - "12000"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
